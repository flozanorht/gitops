pipeline {
    agent {
      any
      //node {
      //  label 'base'
      //}
    }
    stages {
        stage ('host and context') {
            steps {
                sh 'hostname'
                // give it time to check pod creation
                sleep (60)
                sh 'oc whoami'
                sh 'oc whoami -t'
                sh 'oc whoami -c'
                sh 'oc get pod'
            }
        }
    }
}