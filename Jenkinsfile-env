pipeline {
    agent none
    //agent {
      //node {
      //  label 'base'
      //}
    //}
    stages {
        agent any
        stage ('host and context') {
            steps {
                sh 'hostname'
                // give it time to check pod creation
                sleep (30)
                sh 'oc whoami'
                sh 'oc whoami -t'
                sh 'oc whoami -c || oc project test-nobuild'
                sh 'oc get pod'
                // give it time to check pod creation
                sleep (30)
            }
        }
    }
}